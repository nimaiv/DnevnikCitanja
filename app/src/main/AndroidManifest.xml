<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nimai.dnevnikcitanja">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.PridjeliKategorijuActivity" />
        <activity android:name=".view.KategorijaActivity" />
        <activity android:name=".view.LikEditActivity" />
        <activity android:name=".view.LikActivity" />
        <activity android:name=".view.CitatEditActivity" />
        <activity android:name=".view.CitatActivity" />
        <activity android:name=".view.KnjigaTekstActivity" />
        <activity android:name=".view.BiljeskaActivity" />
        <activity android:name=".view.KnjizevnaVrstaActivity" />
        <activity android:name=".view.PisacActivity" />
        <activity android:name=".view.StatistikaActivity" />
        <activity
            android:name=".view.KnjigaActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>
    </application>

</manifest>